[project]
name = "phs"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "ko.libri", email = "entengelb@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "cyclopts>=4.23.3",
    "jinja2>=3.1.6",
    "pydantic>=2.13.5",
    "pyyaml>=6.0.3",
]

[project.scripts]
phs = "phs.cli:run"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
